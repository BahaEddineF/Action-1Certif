name: First workflow

on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo " Hello first github action job "

        - name: list files
          run: ls
        
        - name: Read file
          run: cat README.md